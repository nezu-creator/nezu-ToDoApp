<div class="container">
  <%= form_with(model: @board, url: boards_path, method: 'post', local: true) do |f| %>
    <div>
      <%= f.label :title, 'Name' %>
    </div>
    <div>
      <%= f.text_field :name, class: 'text' %>
    </div>
    <div>
      <%= f.label :title, 'Description' %>
    </div>
     <div>
      <%= f.text_area :description %>
    </div>
    <%= f.submit 'Subumit' , class: 'btn-primary' %>
  <% end %>
</div>